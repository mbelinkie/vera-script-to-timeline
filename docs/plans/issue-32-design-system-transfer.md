# Issue 32 — approved design-system wiring and transfer

## Authority and claim

- Issue: https://github.com/mbelinkie/vera-script-to-timeline/issues/32
- Task: `01a06506-8498-71b0-9fdc-52cd759ab9d4`
- Branch: `codex/issue-32-design-system-transfer`
- Required starting commit: `9a6f8c18d79b1533a4e601fec158099db3642613`.
- Route: `gpt-5.6-sol`, high. Ready/unclaimed inspection and claim passed.
- Dependency #21 is closed and Done. Producer acceptance is recorded at
  https://github.com/mbelinkie/vera-script-to-timeline/issues/21#issuecomment-5519228299.
- Accepted source: Claude project `011eee38-8b6b-48aa-a154-d6c0060d4f23`,
  `VERA remaining token audit.dc.html`, decisions 1–29 and section 12.
- Read the repository instructions, coordination skill, product specification,
  suite design contract and plan, historical implementation record, live
  remaining audit, prior wiring evidence, and T-B decision sheet before edits.

## Scope, exclusions, and dependencies

Wire only accepted #21 dispositions in the Research design artifact. Preserve
existing accepted color/radius/T-A work; wire the already accepted 65 T-B
consolidations without further visual changes. Complete approved spacing,
dimensions, shadow, focus, and responsive wiring. Export a versioned shared
design system and import it into a separate Script-to-Timeline design artifact.
The target may demonstrate adoption, not compose #14 application screens.

No product contracts, fixtures, goldens, accepted tests, production UI, research
data, permissions, deployments, application behavior, Resolve, or Fusion changes.
No new dependencies. No new token decisions, nearest-value consolidation, or
automatic cleanup. Keep private research material out of transfer artifacts.

## Accepted evidence and exceptions

- Before source: `VERA Redesign pre-remaining-token baseline.dc.html`, 5049
  lines, recorded SHA-256 prefix `e72fe3861e9af028`; obtain the full hash before
  using it as an exact identity. Source was recorded as byte-identical to the
  working Research artifact.
- Credit the 58 computed-style captures and 29 responsive-delta measurements
  only where their source and method apply.
- Decision 29 waives extra native captures only for untouched states. Existing
  scaled JPEGs are previews, not pixel evidence. Documentation-only full-height
  screenshots are waived.
- Section 12 records 92 approved-token rows / 82 distinct names, four approved
  output-changing shadow substitutions B9–B12, and 15 exception rows.
- Preserve all 73 literal-value dispositions, documentation/instrumentation
  exclusions, deferred typography, companion shadow, and rejected aliases.
- Decision 3 requires real keyboard focus checks. The two existing focus
  defects remain separate follow-ups; this slice does not accept or fix them.

## Bounded sequence

1. Prove a representative export/import mechanism with existing approved
   tokens, plus native capture and real keyboard capability. Retain the exact
   limitation and stop that method if a capability fails.
2. Pin source versions and create a site/state map using accepted ledgers.
3. Apply small token-family/consumer batches through Claude Design, retaining
   before/after versions and affected checks. Keep visual changes B9–B12
   separately attributable. Stop at any new producer choice.
4. Export the complete versioned shared package, import the exact same package
   into the separate target, and record identity and adoption.
5. Complete the required Research and target checks at 1280 × 800 and
   1024 × 768, then prepare the handoff. No automatic repair loop.

## Checks and producer acceptance

Retain source/package hashes, approved site dispositions, unresolved-reference
checks, element-level computed comparisons, native pixel comparisons for exact
substitutions, explicit before/after shadow comparisons, responsive deltas,
overflow measurements, and real keyboard-focus observations. Run repository
whitespace and scope checks for repository deliverables; broaden testing only
if changed repository code warrants it.

Before moving to In review, supply exact artifact/version links and an ordered
checklist: review dispositions; inspect Research before/after at both widths;
inspect target adoption, focus, responsive behavior and overflow at both widths;
confirm the permitted #14 boundary. Producer reply:
`Accepted issue #32 design-system wiring and transfer.`
The issue remains open until that explicit acceptance is recorded.
